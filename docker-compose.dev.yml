services:
  web:
    extends:
      file: docker-compose.yml
      service: web
    build:
      target: development
    ports:
      - 24678:24678
    volumes:
      - .:/workspace
      - /workspace/node_modules
